/.venv
.env
travelProject/travelProject/TravelProjectDjango.code-workspace
django1test.pem
travelProject/202502005
__pycache__/
*.pyc
travelProject/accounts/__pycache__
*.code-workspace
travelProject/db.sqlite3
env
*.sqlite3
.vscode/settings.json
jdk-23.0.2/
**/venv/
*.pem
# ------------------------------------------------------------------------------
# Python 虛擬環境 (venv) 相關
# (通常會在 Windows / Linux / macOS 環境下產生不同名稱的虛擬環境資料夾)
# ------------------------------------------------------------------------------
.venv/
venv/
env/
ENV/
ENV.bak/
**/venv/

# ------------------------------------------------------------------------------
# Python 中繼檔、編譯快取檔
# (不要把 Python 產生的暫存檔或已編譯好 bytecode 檔上傳到版本庫)
# ------------------------------------------------------------------------------
__pycache__/
**/__pycache__/
*.pyc
*.pyo
*.pyd

# ------------------------------------------------------------------------------
# 環境變數或敏感資訊檔
# (包含 API 金鑰、密碼等機密資料，強烈建議不要放上版本庫)
# ------------------------------------------------------------------------------
.env

# ------------------------------------------------------------------------------
# Django 內建的 SQLite 資料庫檔案
# (若有自行新增的 .sqlite3 檔案也一併忽略)
# ------------------------------------------------------------------------------
*.sqlite3
travelProject/db.sqlite3

# ------------------------------------------------------------------------------
# 憑證 / 金鑰檔
# (包含所有 .pem 檔案與特定的 django1test.pem，避免上傳到版本庫)
# ------------------------------------------------------------------------------
*.pem
django1test.pem

# ------------------------------------------------------------------------------
# Visual Studio Code / 工作區設定
# (VSCode 的設定通常是個人化或專案內部環境化，可視情況忽略)
# ------------------------------------------------------------------------------
.vscode
*.code-workspace
travelProject/travelProject/TravelProjectDjango.code-workspace

# ------------------------------------------------------------------------------
# 其他指定需要忽略的目錄或檔案
# ------------------------------------------------------------------------------
jdk-23.0.2/         # Java (JDK) 目錄
travelProject/202502005  # 專案中提到要忽略的資料夾

# ------------------------------------------------------------------------------
# 若有使用 Node / React 等，可加入以下 (依實際需求添加)
# ------------------------------------------------------------------------------
node_modules/
build/
dist/
*.log
*.tmp
